# Cursor 更新后重新注入汉化并重建快捷方式

## 目标

恢复本机汉化与桌面「Cursor中文」快捷方式。

## 做了什么

Cursor 已升级到 3.18.25，更新覆盖了 `workbench.html` 并删除了 `cursor_hanhua.js`；桌面「Cursor中文.lnk」和本机 VBS 也不在了。已执行 `--an-zhuang`：重新注入汉化、生成 VBS、重建桌面快捷方式。

## 改了哪些文件

- 本机 Cursor 安装目录：重新注入 `workbench.html`、`cursor_hanhua.js`、`usage_live.json`、`product.json` 校验值
- 本机生成（不入库）：`QiDong_Cursor_ZhongWen.vbs`、桌面 `Cursor中文.lnk`

## 怎么验证

1. 桌面出现「Cursor中文」
2. 完全退出 Cursor 后用该快捷方式启动
3. 设置页与仓库菜单为中文，输入框下方有用量条

## 风险/未完成

- 以后 Cursor 一更新，官方安装仍会冲掉注入；必须用「Cursor中文」启动才会自动修
- 仓库代码未改，无需提交
